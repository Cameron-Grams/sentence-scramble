const initialState = {
    originalText : "",
    sentences : []
}

function Reducer( state = initialState, action){
    switch( action.type ){
        default:
            return { ...state }
    }
}